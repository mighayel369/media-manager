version: '3.8'

services:
  backend:
    build: .
    volumes:
      - .:/app
      - /app/node_modules
    ports:
      - "5000:5000"
    env_file:
      - .env
    environment:
      - PORT=${PORT}
      - MONGO_URI=${MONGO_URI}
      - CLIENT_URL=${CLIENT_URL}
      - NODE_ENV=${NODE_ENV}